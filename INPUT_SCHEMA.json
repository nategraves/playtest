{
    "title": "Input schema for the apify_project actor.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "account": {
            "title": "Account",
            "type": "object",
            "description": "JSON Object of account to be cancelled",
            "prefill": {
                "username": "alice@example.com",
                "password": "password1"
            },
            "editor": "json"
        }
    },
    "required": []
}
